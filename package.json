{
    "name": "hannavm_mern",
    "version": "2.0",
    "private": true,
    "scripts": {
        "start": "npm install && concurrently \"cd ./backend && npm install && npm start\" \"cd ./frontend && npm install && npm run dev\""
    },
    "dependencies": {
        "aws-sdk": "^2.1628.0",
        "multer": "^1.4.5-lts.1",
        "multer-s3": "^3.0.1"
    }
}
